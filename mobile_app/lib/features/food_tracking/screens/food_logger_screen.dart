import 'package:flutter/material.dart';

class FoodLoggerScreen extends StatelessWidget {
  const FoodLoggerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Food Logger'),
      ),
      body: const Center(
        child: Text('Food Logger Coming Soon'),
      ),
    );
  }
}
